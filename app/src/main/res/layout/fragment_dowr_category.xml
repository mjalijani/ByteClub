<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="viewModel"
            type="com.game.byteclub.viewModel.dowr.DowrCategoryViewModel" />

    </data>

    <LinearLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/secondary"
        tools:context=".view.fragment.dowr.DowrCategoryFragment">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/category_rv"
            android:adapter="@{viewModel.adapter}"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"/>

        <TextView
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:background="@drawable/bg_rectangle_primary_rounded"
            android:text="Next"
            android:onClick="@{viewModel.presentConfiguration}"
            android:gravity="center"
            android:textColor="@color/secondary"
            android:layout_margin="8dp"

            />

    </LinearLayout>


</layout>